{ inputs, ... }: {
  # FIXME:
  # None at the moment. Additional packages are passed as specialArgs. Reserving overlays to actual changes to nixpkgs.
  # default = final: prev: inputs.self.packages.${prev.system};
}
