# Nix generated files
result
.nix-host
.claude

# OpenTofu / Terraform (infra) — state is kept, encrypted, in dotfiles-private
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.auto.tfvars.json
