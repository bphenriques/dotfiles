# Secrets for auth VM
# TODO: Encrypt with sops after generating the auth VM age key
#
# Required secrets:
# - pocket-id/api-key: API key for Pocket-ID management
# - pocket-id/smtp-password: SMTP password for sending invite emails
# - cloudflare_dns_api_token: Cloudflare API token for ACME DNS challenge

pocket-id:
    api-key: ENC[AES256_GCM,data:PLACEHOLDER,type:str]
    smtp-password: ENC[AES256_GCM,data:PLACEHOLDER,type:str]
cloudflare_dns_api_token: ENC[AES256_GCM,data:PLACEHOLDER,type:str]
