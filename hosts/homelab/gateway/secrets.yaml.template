# Gateway secrets template
# Run: sops hosts/homelab/gateway/secrets.yaml
# to create the encrypted version with these keys:

pangolin:
    server-secret: "generate-with-openssl-rand-base64-32"

cloudflare:
    dns-api-token: "your-cloudflare-api-token-for-dns"
