# The vault's database and its path under its owner's share, paired here so the daemon, the Setup URI
# command and the CouchDB grant cannot drift apart.
{
  owner = "bphenriques";
  subpath = "notes";
  database = "obsidiandb-bphenriques";
}
